# Contributing

## Data changes
Every researched fact should include a source.
Do not overwrite observed data with inferred values.
Use `Unknown` rather than `No` when evidence is absent.

## Product changes
Keep public UX focused on decisions, not model complexity.

## Pull request standard
Explain:
- what changed
- why it changes a decision
- which source supports it
- whether it is observed, forecast or inferred
